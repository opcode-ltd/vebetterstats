# VBS Analytics

Analytics apps responsible for data extraction, data processing  
The output analysis files are then served by the web app

## Extract

The app has multiple data extractors - the generate RAW files in CSV format

## Analyse

The app then analyses the raw CSV files, into JSON files that can be used in the web app
